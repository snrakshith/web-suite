// 1. output based question
const obj = {
  a: [1, 2, 3],
  b: [1, 2],
  c: [1, 2, 3, 4, 5],
};
// op : [1,2,3,4,5]

// const inter = Object.values(obj);
// const flatData = inter.flat();
// const uniqueElem = new Set(flatData);

// console.log(flatData);
// console.log(uniqueElem);

// 2.  output based question

// console.log(a[b]);
// 3.  output based question
const a = {
  x: 1,
  y: 5,
  z: 10,
};

const p = a;
p.x = 5;

// Q1. What is the value of a ?
// Q2. Modify the code to show the original value of a ?
// console.log(a);
